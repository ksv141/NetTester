import "protocol.proto";

package OstProto;

// NetTest Protocol
message NetTest {

    optional uint64    timestamp = 1;
    optional uint64    seqnumber = 2;
}

extend Protocol {
    optional NetTest nettest = 450;
}
